subprojects {
    apply plugin: 'java'
    sourceCompatibility = '1.8'
}
wrapper {
    distributionUrl = 'https://services.gradle.org/distributions/gradle-5.3.1-all.zip'
}

ext.isProdBuild = {
    return project.hasProperty('prodBuild')
}
